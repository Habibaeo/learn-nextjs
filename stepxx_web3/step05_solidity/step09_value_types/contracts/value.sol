// Unsigned integer
uint256 num = 42;

// Boolean
bool isTrue = true;

// Address
address recipient = 0x1234567890123456789012345678901234567890;

// Fixed-point number
fixed16x8 price = 1.23;

// Enum
enum Color { Red, Green, Blue }
Color myColor = Color.Red;

// Byte
bytes2 data = hex"1234";

// Struct
struct Person {
    string name;
    uint age;
}
Person alice = Person("Alice", 30);


// This code defines several variables of different value types.

// num is an unsigned integer with a value of 42.
// isTrue is a boolean with a value of true.
// recipient is an address with a specific value.
// price is a fixed-point number with a value of 1.23.
// myColor is an enum with the value Color.Red.
// data is a byte with the value 0x1234.
// alice is a struct representing a person with the name "Alice" and an age of 30.